This project was build using

- React native expo
- Redux toolkit
- React Hook Form is use to handle form state

Node version
This project is runing of node version 16 (12 >=, <= 16)

All dependencies were installed using yarn

- yarn install

The css library used are:

- Taillwind
- Native wind (To allow tailwind in react native application)

To aid readability because we are usign tailwind

- Tailwind CSS intelliSense VSCode extension: This allow readability of tailwind code base
